const num1 = 20;
const num2 = "20";

console.log(parseInt(num2)); // parseInt() es función que toma un string y lo convierte a un número, si dicho string contiene un número valido para convertir.

console.log(num1.toString()); // .toString es un método para pasar un número a un string
