// Objeto Math

let result

result = Math.PI // Saber el resultado de PI
result = Math.round(2.2) //Para redondear un número
result = Math.ceil(3.1) // Cell siempre redondea hacia arriba
result = Math.floor(3.1) // Floor siempre redondea hacia abajo
result = Math.sqrt(144) // Para sacar la raíz cuadrada de un número
result = Math.abs(-200) // Convierte números negativos a positivos
result = Math.min(1, 2, 3, 4, 5, 0) // Muestra el número mínimo de un listado
result = Math.max(1, 2, 3, 4, 5, 0) // Muestra el número máximo de un listado
result = Math.random() // Mostrar un número random

console.log(result);